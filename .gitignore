
# Guest inquiry exports — contain personal data (names, emails, phones).
# Never commit: this repo is public.
cic-inquiries.csv
*-inquiries.csv
*.csv
*.tsv
.env
.env.*
.DS_Store
island-view-retreat/

# Tooling artifacts
.playwright-mcp/

# Analysis prompts — may reference booking specifics
ivr-pricing-prompt-*.md

# Private-repo worktrees — never commit to this public repo
ivr-content-engine/
